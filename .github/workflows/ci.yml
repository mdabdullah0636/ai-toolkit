name: CI

on:
  push:
    branches:
      - main
      - release-v*
  pull_request:
    branches:
      - main
      - release-v*

jobs:
  build-examples:
    name: 'Build Examples'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Examples
        run: pnpm run build:examples

  prettier:
    name: 'Prettier'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Prettier check
        run: pnpm run prettier-check

  structure:
    name: 'Structure'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run structure validation
        run: pnpm run validate-structure

      - name: Run dependency direction validation
        run: pnpm run arch:deps

      - name: Check capability matrix freshness
        run: pnpm run arch:capabilities:check

  scaffolder:
    name: 'Scaffolder'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run scaffolder golden tests
        run: pnpm --filter create-ai-provider test

      - name: Scaffold probe packages
        run: |
          node tools/create-ai-provider/src/index.js ci-probe-acme -a openai-compatible -m "ci-probe-model" -y --no-install
          node tools/create-ai-provider/src/index.js ci-probe-agent -a harness-acp -e "ci-probe-agent --acp" -y --no-install

      - name: Validate scaffolded structure
        run: pnpm run validate-structure

      - name: Run dependency direction validation
        run: pnpm run arch:deps

      - name: Remove probe packages
        run: |
          rm -rf packages/providers/ci-probe-acme packages/providers/ci-probe-agent
          git checkout -- tsconfig.json

  health:
    name: 'Health'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run health check
        run: pnpm run health-check

  inventory:
    name: 'Inventory'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check inventory freshness
        run: pnpm run inventory:check

      - name: Check capability matrix freshness
        run: pnpm run arch:capabilities:check

  eslint:
    name: 'ESLint'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint check
        run: pnpm run lint

  types:
    name: 'TypeScript'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run TypeScript type check
        run: pnpm run types:full

  bundle-size:
    name: 'Bundle Size Check'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm run build:packages

      - name: Check bundle size
        run: cd packages/core/ai-toolkit && pnpm run check-bundle-size

      - name: Upload bundle size metafiles
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: bundle-size-metafiles
          path: packages/core/ai-toolkit/dist-bundle-check/*.json

  test_matrix:
    name: 'Test'
    runs-on: ubuntu-latest
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: Run tests
        run: pnpm test

  # separate "test" job to set as required in branch protections,
  # as the matrix build names above change each time Node versions change
  test:
    runs-on: ubuntu-latest
    needs: test_matrix
    if: ${{ !cancelled() }}
    steps:
      - name: All matrix versions passed
        if: ${{ !(contains(needs.*.result, 'failure')) }}
        run: exit 0
      - name: Some matrix version failed
        if: ${{ contains(needs.*.result, 'failure') }}
        run: exit 1

  load-time_matrix:
    name: 'Load Time Check'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - module: 'ai-toolkit'
            max-load-time: 100
          - module: '@ai-toolkit/openai'
            max-load-time: 65
          - module: '@ai-toolkit/openai-compatible'
            max-load-time: 65
          - module: '@ai-toolkit/anthropic'
            max-load-time: 65
          - module: '@ai-toolkit/google'
            max-load-time: 65
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm run build:packages

      - name: Measure and check load time for ${{ matrix.module }}
        id: load-time
        working-directory: examples/ai-functions
        run: |
          echo "📦 Measuring load time for ${{ matrix.module }}..."
          pnpm tsx src/benchmark/load-time.ts "${{ matrix.module }}" | tee load-time-output.txt

          # Extract the average time from the output
          AVERAGE_TIME=$(grep "Average:" load-time-output.txt | awk '{print $2}' | sed 's/ms//')

          echo ""
          echo "🔍 Checking threshold..."
          echo "Average load time: ${AVERAGE_TIME}ms"
          echo "Maximum allowed: ${{ matrix.max-load-time }}ms"

          if (( $(echo "$AVERAGE_TIME > ${{ matrix.max-load-time }}" | bc -l) )); then
            echo ""
            echo "❌ Load time check failed!"
            echo "${{ matrix.module }}: ${AVERAGE_TIME}ms exceeds ${{ matrix.max-load-time }}ms threshold"
            echo ""
            echo "To fix this:"
            echo "1. Investigate and optimize slow module initialization"
            echo "2. Update the max-load-time in .github/workflows/ci.yml if the increase is justified"
            exit 1
          else
            echo ""
            echo "✅ Load time check passed!"
            echo "${{ matrix.module }}: ${AVERAGE_TIME}ms is within ${{ matrix.max-load-time }}ms threshold"

            # write result to summary
            echo "- Load Time Check for ${{ matrix.module }}: ${AVERAGE_TIME}ms (Max: ${{ matrix.max-load-time }}ms)" >> $GITHUB_STEP_SUMMARY
          fi

  # separate "load-time" job to set as required in branch protections,
  # as the matrix build names above change each time modules are added/removed
  load-time:
    runs-on: ubuntu-latest
    needs: load-time_matrix
    if: ${{ !cancelled() }}
    steps:
      - name: All matrix versions passed
        if: ${{ !(contains(needs.*.result, 'failure')) }}
        run: exit 0
      - name: Some matrix version failed
        if: ${{ contains(needs.*.result, 'failure') }}
        run: exit 1
